<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>
  <%= f.input :ingredient_id, :collection => Ingredient.all %><br>
  <%= f.input :description %><br>
  <%= f.button :submit %>
<% end %>
